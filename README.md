# Springboot Quartz project

*Project for study only :)*

### Requirements

- Java 11
- Docker
- Docker compose
- Maven

### Running docker

```bash
mvn clean install -DskipTests && docker-compose up
```